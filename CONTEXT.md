# 指标运行时上下文

本上下文定义一个 metric-first BI runtime 的领域语言：用户从已连接的数据源中创建业务指标，并基于这些指标提问和查询数据。

## 语言

**数据源**：
运行时可以探查和查询的数据库连接。一个数据源有一种数据库类型和一组连接配置。
_避免使用_：驱动、连接器、存储

**源表**：
从数据源中发现，并被纳入建模流程的表。一个源表包含多个源字段。
_避免使用_：数据集、Cube 表

**源字段**：
从源表中发现的字段。源字段经过用户确认后，可以成为维度、指标或关联键。
_避免使用_：列、属性

**语义目录**：
用户确认后的业务语义事实来源。语义目录保存指标、维度、关联关系、描述、别名和数据源关系。
_避免使用_：YAML schema、模型文件、元数据 blob

**指标**：
可以被查询的数值型业务计算，例如销售额、订单数、平均客单价。指标由一个或多个源字段和聚合规则定义。
_避免使用_：在指代已保存计算定义时使用 metric

**维度**：
用于对指标进行分组、过滤或切分的描述性或时间性属性。维度通常来自一个源字段。
_避免使用_：属性、分组列

**指标查询**：
用户想问的问题在系统中的确定性表达，包含已确认的指标、维度、过滤条件、时间粒度、排序和限制。
_避免使用_：SQL 查询、自然语言 prompt

**NL2Metric**：
基于语义目录，将用户的自然语言问题解析为指标查询的过程。
_避免使用_：NL2SQL、prompt-to-SQL

**认证指标**：
已经被审核并标记为可信、可复用的指标或指标查询。
_避免使用_：保存查询、报表

## 示例对话

开发者：“用户问按月销售额趋势。销售额已经是一个指标了吗？”

领域专家：“是。销售额是一个指标，定义为订单金额源字段的求和。”

开发者：“模型是否应该直接根据用户问题生成 SQL？”

领域专家：“不应该。NL2Metric 应该先把问题解析到语义目录中的销售额指标和订单月份维度。只有在指标查询确定后，系统才生成 SQL。”

开发者：“用户可以不编辑 YAML schema 就新增指标吗？”

领域专家：“可以。用户通过建模体验创建或确认指标，结果持久化到语义目录中。”
